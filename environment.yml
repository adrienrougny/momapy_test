name: momapy_test
channels:
  - defaults
dependencies:
  - python=3.10
  - pip:
    - libsbgnpy
    - frozendict
    - pycairo
    - pygobject
    - numpy
    - pyparsing
    - skia-python
    - xsdata[cli, lxml, soap]
    - bezier",
    - shapely~=2.0rc1
    - ipykernel
variables:
  BEZIER_NO_EXTENSION: True
